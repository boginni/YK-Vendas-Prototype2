name: forca_de_vendas
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.0-116.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  cloud_firestore: ^3.1.1


  # NumberFormat
  intl: ^0.17.0

  # Forms formatados com padrão brasileiro
  brasil_fields: 1.3.0



  #firebase_auth: ^3.3.0
  #firebase_storage: ^10.2.0
  #firebase_core: ^1.10.1

  provider:
  sqflite:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
