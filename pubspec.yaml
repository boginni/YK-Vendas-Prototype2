name: forca_de_vendas
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.0-116.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons:
  cloud_firestore: ^3.1.1
  #0.13.5
  firebase_auth: ^3.3.0
  #0.16.0
  firebase_storage: ^10.2.0
  #3.1.5
  firebase_core: ^1.10.1
  #0.5.0
  provider:
  sqflite:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
